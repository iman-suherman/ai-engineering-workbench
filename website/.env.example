# Contact form email — same Gmail SMTP account as alocare-notification-service
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=suherman.fb@gmail.com
SMTP_PASS=
EMAIL_FROM_NAME=AI Engineering Workbench
EMAIL_FROM_ADDRESS=suherman.fb@gmail.com

# Optional: load SMTP_PASS from alocare checkout when unset here
# ALOCARE_NOTIFICATION_SERVICE_ROOT=~/src/alocare.ai/alocare-notification-service

# Admin notifications always go to iman.suherman@gmail.com (hardcoded in server)

# Optional: keep Cloud Run warm (reduces cold-start latency)
# WORKBENCH_MIN_INSTANCES=1
