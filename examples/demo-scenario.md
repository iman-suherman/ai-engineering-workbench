Can we push the edge gateway change for checkout-api this week?
Is anyone actively developing on checkout or payments?

Context (fictional demo platform):
- checkout-api serves cart and checkout HTTP APIs
- edge-gateway-config holds route rules for the edge gateway
- payments-api handles authorization and capture

We need to know if deploying the gateway config is safe before Friday's release window.
