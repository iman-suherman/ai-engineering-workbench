# Architecture overview (sample)

This is a **fictional** sample so the workbench runs without proprietary data.

```text
Client → edge-gateway → checkout-api → payments-api
                              ↓
                      inventory-service
```

Replace this diagram and the related docs with your real platform architecture.
